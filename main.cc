#include <iostream>
#include <fstream>
#include <vector>
#include <algorithm>
#include "./include/ctl.cc"
using namespace std;

// main is still worthless

int main() {
  Ctl s = Ctl();
  s.loop();
  //s.remove_dot(testdot);
}
